# Django secret key (change it for production)
SECRET_KEY=django-insecure-aslivo-store-secret-key-change-in-production

# Debug mode (set to False in production)
DEBUG=True

# Email credentials for SMTP (use app passwords for Gmail)
EMAIL_HOST_USER=muhammadaslamlaskani01@gmail.com
EMAIL_HOST_PASSWORD=balochlaskani

# Google OAuth (if applicable)
GOOGLE_CLIENT_ID=xxxxxxxxxxxx.apps.googleusercontent.com  # Replace with real client ID

# Database connection (use PostgreSQL in production)
DATABASE_URL=postgres://your-db-username:your-db-password@your-db-host:your-db-port/your-db-name  # Replace with actual PostgreSQL credentials

# React API URL (for frontend to backend communication)
REACT_APP_API_URL=https://ecomlaunchtest.onrender.com